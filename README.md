# Angular Tech Test Skeleton

## Introduction

Simple skeleton project for an Angular tech test, using Webpack and including Boostrap.

## Usage

```
cd src
npm install
npm start
```

Webpack will auto-reload when JS or CSS changes (not currently HTML).
